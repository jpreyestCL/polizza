import "server-only";
import { createSimulatedAdapter } from "./simulated";

/** Sura Seguros — reemplazar por scraper real. */
export const suraAdapter = createSimulatedAdapter("sura", "Sura Seguros");
