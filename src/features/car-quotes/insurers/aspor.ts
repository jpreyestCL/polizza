import "server-only";
import { createSimulatedAdapter } from "./simulated";

/** Aspor Seguros — reemplazar por scraper real. */
export const asporAdapter = createSimulatedAdapter("aspor", "Aspor Seguros");
